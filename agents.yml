# A list of AI agents the orchestrator can use.
# The '{prompt}' placeholder will be replaced by the -m argument.
# The '{worktree_path}' placeholder gives the agent context.

agents:
  - name: "codex"
    command: "codex exec -C {worktree_path} --full-auto -- {prompt}"
    
  - name: "cursor"
    command: "cursor-agent chat {prompt}"

  - name: "claude"
    command: "claude -p --permission-mode acceptEdits -- {prompt}"
